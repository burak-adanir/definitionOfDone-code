#include <stdio.h>
#include "source.h"
void main()
{   int x;
    int meineZahl = 0;printf ("How many numbers do you want");scanf ("%d", &meineZahl);meineZahl = 0;

    for (x = 1; x< deineZahl(); x++)
    {
        printf("%d\t", x);
    }
}
